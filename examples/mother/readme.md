## CONCEPT
Mother node is a node that has the capacity to create, validate and publish other nodes
It relies on /llm.request, /llm.response to query LLM agent, has access to the linux terminal (analogous to llm_execute_memory)
and upon extensive testing and verification and testing publishes it to the registry